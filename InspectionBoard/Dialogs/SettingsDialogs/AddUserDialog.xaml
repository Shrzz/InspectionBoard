<UserControl x:Class="InspectionBoard.Dialogs.SettingsDialogs.AddUserDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:InspectionBoard.Dialogs.SettingsDialogs"
             xmlns:utility="clr-namespace:Authorization.Utility;assembly=Authorization"
             mc:Ignorable="d" 
             Height="450" Width="400">
    <Grid>
        <materialDesign:Card Padding="19" >
            <StackPanel>
                <StackPanel VerticalAlignment="Center" Margin="0 0 0 0">
                    <TextBox Style="{StaticResource MaterialDesignFloatingHintTextBox}" 
                             materialDesign:HintAssist.Hint="Логин"
                             Margin="20 0 20 0"
                             Text="{Binding User.Username}"/>
                    <PasswordBox  utility:PasswordBoxAssistant.BindPassword="True"
                                  utility:PasswordBoxAssistant.BoundPassword="{Binding User.Password, Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" 
                                  materialDesign:TextFieldAssist.HasClearButton="True"  
                                  materialDesign:HintAssist.Hint="Пароль" 
                                  Margin="20 15 20 0" 
                                  Style="{StaticResource MaterialDesignFloatingHintPasswordBox}"/>
                    <Button Margin="20 10 20 0"
                        Content="Добавить"
                        Command="{Binding CloseDialogCommand}"
                        CommandParameter="true"/>
                    <Button Margin="20"
                        Content="Отмена"
                        Command="{Binding CloseDialogCommand}"
                        CommandParameter="false"/>
                </StackPanel>
            </StackPanel>
        </materialDesign:Card>

    </Grid>
</UserControl>
