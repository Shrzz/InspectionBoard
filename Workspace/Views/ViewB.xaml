<UserControl x:Class="Workspace.Views.ViewB"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Workspace.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="#FFFEDBD0" Margin="100 100 100 100">
    <Grid Margin="100">
        <TextBlock VerticalAlignment="Top" HorizontalAlignment="Center" Text="{Binding Message}"/>
        <ComboBox ItemsSource="{Binding Specialities}" SelectedItem="{Binding SelectedItem}" />
        <Button Content="Выбрать"  HorizontalAlignment="Center" VerticalAlignment="Bottom" Command="{Binding NavigateCommand}" Margin="170,0,342,19.6"/>
        <Button Content="Назад"  HorizontalAlignment="Center" VerticalAlignment="Bottom" Command="{Binding NavigateCommand}" Margin="365,0,164,19.6"/>
    </Grid>
</UserControl>
