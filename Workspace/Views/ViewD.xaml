<UserControl x:Class="Workspace.Views.ViewD"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Workspace.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <ComboBox ItemsSource="{Binding Specialities}" SelectedItem="{Binding SelectedItem}" />
        <Button Content="Delete"  HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="220,0,507,61.6" RenderTransformOrigin="0.135,1.775"
                Command="{Binding DeleteSpecialityCommand}"/>
        <Button Content="Back" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="495,0,228,61.6" Width="77"
                Command="{Binding NavigateCommand}"/>
        <TextBlock Text="{Binding Message}" VerticalAlignment="Top"/>
    </Grid>
</UserControl>
